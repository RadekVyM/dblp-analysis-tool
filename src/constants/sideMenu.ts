/** Classes that should be applied when the side menu is docked. */
export const DOCKED_SIDE_MENU_CLASSES = ['grid-cols-[1fr_0]', 'md:grid-cols-[1fr_var(--side-bar-width)]', 'md:gap-x-5'];
/** Classes that should be applied when the side menu is floating or hidden. */
export const UNDOCKED_SIDE_MENU_CLASSES = ['grid-cols-[1fr_0]'];