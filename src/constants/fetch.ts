/** Separator of JSON objects that are streamed to the client. */
export const STREAMED_OBJECTS_SEPARATOR = '\n';
/** How long the application should wait between requests to dblp.org. */
export const WAITING_TIME_BETWEEN_DBLP_REQUESTS = 1500;