import LoadingPage from '@/components/shell/LoadingPage'

/** Loading component for the author group publications page. */
export default function Loading() {
    return (
        <LoadingPage />
    )
}